var React = require('react');

var TextEditor = React.createClass({
  render: function() {
    return (
      <h1> A new text editor to rule them all. </h1>
    );
  }
});

module.exports = TextEditor;
